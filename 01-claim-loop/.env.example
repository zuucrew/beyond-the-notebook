# Copy to .env and fill in. .env is gitignored and must stay that way.

# Local Postgres over the unix socket as your own macOS user.
DATABASE_URL=postgresql:///claimloop

# Which extractor to use: "groq" reads the PDF for real, "stub" reads the
# ground-truth JSON beside it and corrupts it deterministically (free, offline,
# repeatable -- useful for testing the queue without spending anything).
EXTRACTOR=groq

GROQ_API_KEY=
GROQ_BASE_URL=https://api.groq.com/openai/v1

# Verify against Groq's current catalogue -- model ids move, and the
# vision-capable list is shorter than the text one.
GROQ_MODEL=qwen2.5-vl-72b-instruct

# Rasterisation. Image tokens scale with pixel area, so DPI and page count are
# both direct cost levers.
PDF_DPI=150
MAX_PAGES=8

# D-007 — the escalation threshold. Change it and watch the auto-approve rate
# move. A number to argue with, not to defend.
CONFIDENCE_THRESHOLD=0.80

# How long a worker or reviewer may hold a claim (seconds).
# Set it to 10 to watch the reaper do its job.
LEASE_SECONDS=900

# Attempts before a claim goes to extraction_failed.
MAX_ATTEMPTS=3

# Connection pool, per process. instances x DB_POOL_MAX must stay under the
# database's max_connections -- roughly 25-50 on the smallest Cloud SQL tiers.
# On serverless, 1 or 2 is normal.
DB_POOL_MIN=1
DB_POOL_MAX=5
