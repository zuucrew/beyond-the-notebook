"""Business rules. Imports nothing outside this package.

No database, no network, no framework — so everything here is testable with a
plain function call. If something in this layer needs a connection or an API
key, it is in the wrong layer.
"""
